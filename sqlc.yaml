version: "2"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/product/queries/"
    gen:
      go:
        package: "product"
        out: "./internal/product/sqlc/"
        sql_package: "database/sql"
